# temp_4_gpt 仓库协作规则

## 目录职责

- `减脂/每日记录/`：每日原始事实的唯一正式来源，使用 `YYYY-MM-DD.md` 命名。
- `减脂/周报/`：按周汇总体重、饮食、训练、恢复和调整计划。
- `减脂/体重与围度记录.md`：长期体重／围度测量表，不重复记录完整饮食和训练。
- `减脂/常用食物.md`：包装标签、固定配方、通用参考值及可信度说明。
- `减脂/模板/`：每日记录和周报的复制来源，不直接填写真实数据。
- `骑行训练/`：春节骑行目标、月度／周计划、路线规划和复盘资料。
- `脚本/`：可复用用户脚本和其他脚本文件。
- `临时/`：网页版短期文件，仅用于周转，不作为正式归档位置。

## 减脂记录规则

1. 新增每日记录必须复制 `减脂/模板/每日记录模板.md`，并重命名为 `减脂/每日记录/YYYY-MM-DD.md`。
2. 每日记录保存当天实际测量、饮食、训练和身体反馈；没有发生或没有记录时，明确填写“无”“不适用”或“尚未记录”。
3. 每日记录按“体重与围度 → 睡眠与状态 → 饮食 → 营养估算 → 训练与活动 → 身体反馈 → 当日复盘”的结构填写。
4. 原始事实、营养估算和复盘建议分开；不把估算值写成精确实测值。
5. 包装营养标签优先于通用参考值；标签不完整时不要反推出未提供的营养成分。
6. 固定配方和常用食物先登记到 `减脂/常用食物.md`，当天实际品牌、配方或分量变化时，以当日记录为准。
7. 固体优先记录可食部分的 `g`，液体使用 `ml`；带壳、带骨或宰杀前重量不能直接等同于可食重量。
8. 每日记录完成后，将新测得的体重／围度追加到 `减脂/体重与围度记录.md`。
9. 每周结束后复制 `减脂/模板/周报模板.md`，在 `减脂/周报/` 中建立对应周报；周报只做汇总，不替代每日记录。
10. 不根据单日体重或围度波动直接判断脂肪变化，优先观察 7 日体重平均和 2～4 周围度趋势。

## 计划与临时文件规则

- 骑行月度计划确定阶段重点，周计划根据天气、身体、车辆和上一周完成情况动态调整。
- 计划是预期，实际结果以复盘记录为准；未完成目标时记录原因，不通过冒险补偿里程。
- `临时/` 中的文件使用后及时删除或迁移到正式目录。
- 不要在仓库中提交密码、令牌、Cookie、个人身份信息或其他未准备公开的敏感内容。
- 本仓库目前为公开仓库，减脂记录包含个人健康信息；如不希望公开，应优先将仓库改为私有或迁移到私有仓库。

## 修改范围

- 保留已有事实记录，不随意删除历史数据。
- 修改营养数据时保留数据来源、单位、参考分量和可信度。
- 修改目录或文件名时同步更新相关 README 和 Markdown 链接。
- 完成修改后运行 `git diff --check`，并检查文件树与内部链接。
