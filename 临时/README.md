# 临时文件

本目录用于网页版临时存放短期文件，例如临时导出的 Markdown、网页调试材料或待分类资料。

## 使用规则

- 只存放短期文件，不在这里维护正式资料。
- 文件名尽量包含用途或日期，便于清理。
- 使用完成后及时删除，或迁移到 `减脂/`、`骑行训练/`、`脚本/` 等正式目录。
- 不要放置密码、令牌、个人身份信息或其他不应公开的内容。

注意：文件一旦提交到 Git，删除当前文件也不会自动删除 Git 历史。
